<p id="notice"><%= notice %></p>
<div class="box box-default">
<div class="box-header with-border">
    <h3 class="box-title">
      <strong>
       Dogs Cadastred
      </strong>
    </h3>
</div>

  <div class="box-body">
    <div class="row">
    <div class="col-md-12">

  <%= form_tag(dogs_path, method: "get", class: "form-inline") do |f| %>
  <div class="row">
   <div class="col-md-12">
    <div class="form-group">

      <%=select_tag "genre", options_for_select([ "Female", "Male" ],params[:genre]),prompt: "Select a genre",class:"form-control" %>

  </div>

    <div class = "form-group">

      <%=select_tag "breed", options_for_select(@breeds,params[:breed]),prompt: "Select a breed",class:"form-control" %>

    </div>

    <div class="form-group">
      <%= text_field_tag :name, nil, value: params[:name], placeholder: "Name", id: "nome" ,class:"form-control" %>
    </div>

    <div class="form-group">
      <%= select_tag :castrated,options_for_select([ "Yes", "No" ]),prompt: "Castrated?",class:"form-control"  %>
    </div>

      <div class="form-group">
      <%= text_field_tag :owner, nil, value: params[:owner], placeholder: "owner", id: "owner" ,class:"form-control" %>
    </div>

    <div class="form-group">
      <%= submit_tag "Search",class: "btn btn-primary btn-sm"%>
    </div>


  </div>
  </div>
<% end %>
</div>
</div>

&nbsp;
<div class="row">
<div class="col-md-12">

  <div class="text-center">
    <div class="row">
        <%= will_paginate @dogs, :page_links => true ,class: "apple_pagination"%>
    </div>
  </div>



<table class="bootstrap-table">
  <thead>
    <tr>
      <th>Breed</th>
      <th>Genre</th>
      <th>Castrated?</th>
      <th>Birthday</th>
      <th>Name</th>
      <th>Owner's name</th>
      <th>Owner's fone</th>
      <th>Last date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dogs.each do |dog| %>
      <tr>
        <td><%= dog.breed %></td>
        <td><%= dog.genre %></td>
          
        <%if dog.castrated == true%>
          <td>  Sim </td>

          <%else%> 

          <td> Não </td>

        <% end %>

        <td><%= l dog.birthday %></td>
        <td><%= dog.name %></td>
        <td><%= dog.owner_name %></td>
        <td><%= dog.owner_fone %></td>
        <td><%= l dog.last_date %></td>
        <td><%= link_to dog do%>
          <i class="fa fa-eye"></i>
          <% end %>
        </td>

        <td><%= link_to edit_dog_path(dog) do%>
          <i class="fa fa-edit"></i>
          <% end %>
        </td>
        
        <td><%= link_to dog, method: :delete, data: { confirm: 'are you right?' } do%>
            
          <i class="fa fa-close"></i>

          <% end %>

        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>
</div>
<%= link_to 'New Dog', new_dog_path ,:class =>'btn btn-success'%>
</div>
</div>

